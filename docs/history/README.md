# Historical material

Historical release documents and compatibility entrypoints may remain for reproducibility. Their presence does not make them current instructions. Current authority is defined in `AI_BOOTSTRAP.md` and `manifest.json`.
