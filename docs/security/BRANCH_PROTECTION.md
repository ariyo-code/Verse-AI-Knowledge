# Recommended main-branch protection

A template is provided at `.github/rulesets/main-recommended.json`.

Recommended policy: block force pushes/deletion, require CI, require conversation resolution, prefer linear history, and require PR review for external contributions.

**MANUAL ACTION REQUIRED:** repository rulesets are account/repository settings. Confirm the exact current CI check names before applying the template.
