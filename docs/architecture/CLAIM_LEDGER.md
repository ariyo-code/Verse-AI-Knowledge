# Claim Ledger

The Claim Ledger is generated after an Evidence Pack.

It records which technical claims are currently supported.

Important examples:

- exact API signatures require signature/API-page evidence;
- local `compiled` requires actual local UEFN compile evidence;
- external GitHub examples can support patterns and upstream claims only;
- planned architecture can support intent, not exact API syntax.
