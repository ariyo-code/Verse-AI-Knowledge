# V24 → V25 migration

V25 changes the repository release and schema to 25 while preserving V24 evidence semantics.

Main changes: canonical prompt compiler, current/legacy prompt split, SDK/CLI expansion, stronger Python/package quality, MCP knowledge interface, release automation, static docs, security/GitHub hygiene, and expanded evaluation infrastructure.

Existing V21/V23/V24 portable prompts remain archived for compatibility. Use `AI_BOOTSTRAP.md` and `portable/current/` for new work.
