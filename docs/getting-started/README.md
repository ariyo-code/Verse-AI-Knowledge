# Getting started

1. Read `AI_BOOTSTRAP.md`.
2. Install with `python -m pip install -e .`.
3. Run `verse-ai doctor`.
4. Search with `verse-ai search "query"`.
5. Resolve exact API claims with `verse-ai claim SYMBOL --field signature`.
6. Use `TODO(API VERIFY)` for unsupported exact claims.
