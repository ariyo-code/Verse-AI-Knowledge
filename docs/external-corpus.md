# External corpus

Third-party material is always lower authority than current official evidence for exact Verse API claims.

Upstream compile claims may be stored as source provenance, but they do not become local `compiled` validation without a real local UEFN compilation. `reference-only` sources are not vendored.
