# API versioning

V25 keeps the current snapshot in `knowledge/api/versions.json` and prepares versioned comparisons through `verse-ai api-diff OLD NEW`. A diff is only as trustworthy as the verified data in the compared snapshots.
