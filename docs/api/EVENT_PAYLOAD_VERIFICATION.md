# Event payload verification

Event names, payload types, tuple structures, optional payloads and listenable signatures are independent exact claims. A handler signature must not be generated as certain when payload evidence is absent; use `TODO(API VERIFY)`.
