# VerseLab Offline Batch 001

V16 embeds 10 real Verse source files fetched from the pinned UEFN Central revision.

They can be staged without synchronizing the full repository.

Run:

```powershell
.\lab\next-candidate.ps1 `
  -VerseDir "C:\Path\To\VerseLab\Verse"
```

These files still have **no local UEFN compile proof**. Their local status remains pending until you compile them.
