# Self-hosted UEFN runner preparation

V25 only prepares the contract. No UEFN runner was executed for this release.

A Windows self-hosted runner may later stage one candidate, compile Verse, capture compiler output and write immutable evidence under `verification/`. Runtime and multiplayer tests remain separate stages.
