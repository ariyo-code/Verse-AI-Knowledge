# Provider adapters

Provider-neutral interface for reproducible LLM evaluation. No API key belongs in this repository. `saved_responses.py` supports offline replay; live adapters are optional and must read credentials from the caller environment.
