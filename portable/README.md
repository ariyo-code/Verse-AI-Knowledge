# Portable prompts

`portable/current/` is the only current portable generation. It is compiled from `prompt_sources/` by `tools/build_prompts.py`.

Historical generations are retained under `portable/archive/` for compatibility and provenance. Repository-aware agents must start with `AI_BOOTSTRAP.md`.
